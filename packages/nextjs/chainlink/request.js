const getRequestConfig = require("./Functions-request-config.js");
const { buildRequest } = require("./FunctionsSandboxLibrary/buildRequest");

const request = async (nftAddress, tokenId) => {
  const requestConfig = getRequestConfig(nftAddress, tokenId);
  const request = buildR;
};
